# Lacunas

O que foi procurado e não encontrado durante a F1. Cada linha vira pergunta obrigatória na F2.

| # | Lacuna | Onde procurei |
|---|---|---|
| L-01 | **Nenhuma infraestrutura de teste existe.** Não há projeto de teste, framework declarado, runner, fixture ou comando. A regra 3 do método exige TDD e a regra 13 manda a primeira sprint entregar a capacidade de testar — o escopo, o framework (DUnitX?) e a versão de Delphi alvo não estão definidos em lugar nenhum. | Varredura por `*test*`/`*dunit*` em toda a árvore; `boss.json` (`"projects": []`); `CLAUDE.md` ("no standalone build, lint, or test command in this repo"). |
| L-02 | **Não há `.dproj`/`.dpk` versionado**, logo não há como compilar a lib isoladamente hoje. Um projeto de teste precisará declarar a versão de Delphi e as units de FireDAC — nenhuma das duas está registrada. | `ls *.dpk *.dproj *.dpr` na raiz (vazio); só resíduos ignorados (`Connection.dproj.local`, `__history/Connection.dpk.~1~`). |
| L-03 | **`plano_connection.md` não existe**, mas é citado duas vezes pelo `CLAUDE.md` como documento vivo a ser lido antes de editar. A SPEC ("Pontos em aberto" nº 2) levanta que ele pode existir em outro checkout e conter achados em arquivos não lidos pela auditoria. Não há como verificar daqui. | `ls plano_connection.md`; busca pelo nome em toda a árvore (só ocorre dentro do próprio `CLAUDE.md`). |
| L-04 | **Ambiente de reprodução de R-01 não está disponível.** A SPEC afirma que o defeito foi reproduzido em bancada contra Firebird 5.0 real com um `Diag2.dpr` de 15 linhas — esse arquivo não existe neste repositório, e não há instância de Firebird, `config.ini` nem `config.ini.exemplo` aqui. | Varredura da árvore por `Diag2`, `*.ini`; `ls` da raiz e de `src/`. |
| L-05 | **Permissão de push para `github.com/russimar/connection` não verificada.** A SPEC coloca isso como ponto em aberto nº 1, com a alternativa de vendorizar a lib caso não exista acesso. O diretório de trabalho é um clone com `main` limpo, mas isso não confirma permissão de escrita. | `git remote`/`git log` (HEAD `d497f21`, árvore limpa); `docs/bug-biblioteca-conexao.md`, "Pontos em aberto" nº 1. |
| L-06 | **Divergência técnica entre a SPEC e a semântica do Delphi, não resolvida.** A SPEC (A-01) afirma que `FParent.Connection as TFDCustomConnection` levantaria `EInvalidCast` se `Connection` devolvesse `nil`; em Object Pascal, `nil as T` devolve `nil` sem exceção. O achado continua válido, mas o sintoma esperado muda (falha tardia no `Open`, não `EInvalidCast`) — e isso altera qual teste comprova a correção. | `src/Query/Provider.Query.pas:57` confrontado com `docs/bug-biblioteca-conexao.md`, seção A-01. |
| L-07 | **Versão declarada no `boss.json` (`1.0.0`) não acompanha as tags (`v1.0.8`).** Não está documentado se o campo deve ser atualizado a cada release nem qual será o número da versão que sai com estas correções. | `boss.json:4`; `git tag -l`. |
| L-08 | **Consumidores da lib não são enumeráveis daqui.** A SPEC cita o AFSelf e menciona "projetos servidores da empresa" que usariam `TGerenciadorConexao`, sem nomeá-los. Isso determina quanto de compatibilidade retroativa as correções precisam preservar (ex.: manter ou não o fallback `'PDV'` de A-01, manter ou não a precedência de `parceiro.ini` de A-03). | `docs/bug-biblioteca-conexao.md` (seções A-01, A-02, A-03); `README.md`; `boss.json`. |
| L-09 | **Campos `Usuario` e `Senha` de `TDadosConexao` nunca são preenchidos** por `BuscarParametro`, e nenhuma unit deste repositório os lê. A origem e a finalidade desses campos (a SPEC os chama "herdados de outro uso") não estão documentadas — não dá para saber se podem ser removidos sem quebrar consumidor. | `src/Provider.DadosConexao.pas:11-12`; varredura por `Usuario`/`Senha` em `src/`. |
